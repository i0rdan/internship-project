export interface Cookbook {
  label: string,
  author: string,
  description: string,
  photo: string,
  likes: string[],
  comments: number,
  views: string[],
  recepiNames: string[],
  type: string
}